# Сервер `188.124.55.89` (хост `Bohr`) — инвентаризация возможностей

Документ составлен **по фактам**, полученным с машины по SSH (состояние на момент съёмки: **2026-04-04**). При изменении конфигурации разделы нужно перепроверить командами из конца файла.

---

## 1. Назначение в общей схеме

Этот сервер выступает как **GPU/AI-узел**: локальный запуск **ComfyUI** (генерация изображений), векторное хранилище **Qdrant** для RAG/эмбеддингов, сервис **Ollama** для LLM и эмбеддингов.  
**Веб-приложение, Nginx, PM2, основной API** в типичной схеме проекта размещены на другом хосте (например `89.169.39.244`); на `188.124.55.89` в снятом состоянии **не** обнаружены активные `nginx` и `pm2`.

---

## 2. Система

| Параметр | Значение (факт) |
|----------|------------------|
| Hostname | `Bohr` |
| ОС | Ubuntu 22.04 LTS (`6.8.0-106-generic`) |
| Архитектура | `x86_64` |
| Uptime (на съёмке) | ~7 дней |
| Load average (пример) | `0.21, 0.14, 0.07` |

### Память и диск

| Ресурс | Значение |
|--------|----------|
| RAM | ~31 GiB всего, ~15 GiB доступно (кэш учтён) |
| Swap | ~4.7 GiB |
| Корневой раздел | `ext4` на RAID-массиве `md44`, **~433 GiB**, занято **~105 GiB** (~26%) |

### Дисковая раскладка (кратко)

- Зеркало RAID по `sda`/`sdb`: boot EFI, `/boot`, swap, корень `/` на `md44`.

---

## 3. Сеть

| Интерфейс | Адрес |
|-----------|--------|
| `eth1` (основной публичный) | `188.124.55.89/24` |
| `docker0` | `172.17.0.1/16` (мост Docker) |

### Слушающие порты (релевантные)

По `ss -tlnp` на съёмке:

| Порт | Процесс / назначение |
|------|----------------------|
| `22` | `sshd` — удалённый доступ |
| `53` | `systemd-resolved` (локальный DNS stub, `127.0.0.53`) |
| `8188` | **ComfyUI** (контейнер `comfyui`, проброс `8188→8188`) |
| `6333`, `6334` | **Qdrant** (HTTP и gRPC соответственно, типичная схема) |
| `11434` | **Ollama** API (слушает на `*:11434`) |
| `44629`, `40471` | локальные **ollama runner** (привязка к `127.0.0.1`) |

---

## 4. Firewall (UFW)

Статус: **active**.

Пример правил (факт с сервера):

- **SSH (OpenSSH)** — разрешён с Anywhere (IPv4/IPv6).
- **`8188/tcp`** — разрешён (ComfyUI), в т.ч. для IPv6.
- **`6333/tcp`, `6334/tcp`** — разрешены (Qdrant).
- **`11434/tcp`** — разрешён (Ollama), с комментарием в UFW.

Итог: снаружи доступны **ComfyUI 8188**, **Qdrant 6333/6334**, **Ollama 11434**.

---

## 5. Docker

Сервис `docker.service` — **active (running)**.

### Контейнеры (факт)

| Имя | Образ | Статус | Порты на хосте |
|-----|--------|--------|----------------|
| `comfyui` | `ghcr.io/ai-dock/comfyui:latest` | Up (дни) | `0.0.0.0:8188→8188` |
| `qdrant` | `qdrant/qdrant:latest` | Up (дни) | `0.0.0.0:6333-6334→6333-6334` |

Внутренние IP в bridge-сети (пример): `comfyui` → `172.17.0.3`, `qdrant` → `172.17.0.2`.

### Тома / каталоги на хосте (bind mounts)

**`comfyui`:**

- `/opt/comfyui-data/output` → `/opt/ComfyUI/output`
- `/opt/comfyui-data/models` → `/opt/ComfyUI/models`

Именованных Docker volumes (`docker volume ls`) в снятом списке **не было** — используются привязки к путям на диске.

### Проверка HTTP (локально на сервере)

- `http://127.0.0.1:8188/` → **HTTP 200**

### Python / стабильность ComfyUI (связь с `imageWorker` на MAIN SERVER)

- Часть custom nodes (в т.ч. `comfyui_controlnet_aux`) может требовать **`matplotlib`**. Если в логах `imageWorker` на проде видно `No module named 'matplotlib'`, в контейнере: `docker exec comfyui pip install matplotlib`, при необходимости `docker restart comfyui`.
- Ошибка ComfyUI **`[Errno 32] Broken pipe`** в ответе history обычно означает падение воркера выполнения графа (VRAM/CUDA, сбой Python). Смотреть `docker logs comfyui` на момент запроса.
- Аудит текстового графа (как у Telegram «Генерация»): на MAIN SERVER из `apps/api` выполнить `node scripts/e2e-comfy-text-audit.js` (нужен доступ к `COMFYUI_URL`, по умолчанию этот хост `:8188`).

---

## 6. Qdrant (векторная БД)

- Слушает **`0.0.0.0:6333`** (HTTP API) и **`6334`** (gRPC).
- Локальная проверка: `GET http://127.0.0.1:6333/` → **HTTP 200**.

### Коллекции (факт с `/collections`)

Пример имён коллекций на съёмке:

- `asst_dd82095192dd40099c6d3cdbf858f2ab`
- `asst_e8bddeb46d284cef8a8b3522f880c49e`
- `asst_eebd8b990b17477399a0d3b08a13557d`
- `asst_2bce0ef74703464e941551c6fe330149`
- `knowledge_chunks`

Это соответствует использованию **RAG / knowledge base** (в т.ч. ассистентские коллекции по шаблону `asst_*`).

---

## 7. Ollama (LLM / embeddings)

- **Версия:** `0.18.3` (факт: `ollama --version`).
- Юнит **`ollama.service`** — **enabled**, **active (running)**.
- Основной процесс: `/usr/local/bin/ollama serve`.
- Дополнительно видны **runner**-процессы на локальных портах (например `127.0.0.1:44629`, `127.0.0.1:40471`) — типично при загруженных моделях.

### Полный перечень моделей на сервере (факт)

На съёмке в Ollama установлено **ровно 6 моделей** — других записей в `ollama list` нет.

Сводная таблица (источник: `ollama list` + `GET http://127.0.0.1:11434/api/tags`):

| № | Имя в Ollama | Размер на диске | Формат | Семейство | Параметры | Квантизация | Digest (короткий) |
|---|----------------|------------------|--------|-----------|-----------|-------------|-------------------|
| 1 | `qwen2.5:7b` | ~4.36 GiB (4 683 087 332 B) | GGUF | qwen2 | **7.6B** | **Q4_K_M** | `845dbda0ea48…` |
| 2 | `nomic-embed-text:latest` | ~262 MiB (274 302 450 B) | GGUF | nomic-bert | 137M | F16 | `0a109f422b47…` |
| 3 | `mixtral:latest` | ~24.6 GiB (26 443 602 516 B) | GGUF | llama | **46.7B** | Q4_0 | `a3b6bef0f836…` |
| 4 | `codellama:latest` | ~3.56 GiB (3 825 910 662 B) | GGUF | llama | **7B** | Q4_0 | `8fdf8f752f6e…` |
| 5 | `mistral:latest` | ~4.07 GiB (4 372 824 384 B) | GGUF | llama | **7.2B** | Q4_K_M | `6577803aa9a0…` |
| 6 | `llama3:8b` | ~4.34 GiB (4 661 224 676 B) | GGUF | llama | **8.0B** | Q4_0 | `365c0bd3c000…` |

**Назначение по типу задач (обобщение, не настройка приложения):**

- **Чат / рассуждения / многоязычный текст:** `qwen2.5:7b`, `mistral:latest`, `llama3:8b`, `mixtral:latest` (Mixtral — MoE, самая тяжёлая по VRAM/диску).
- **Код:** `codellama:latest`.
- **Эмбеддинги для RAG (векторизация текста):** `nomic-embed-text:latest` — обычно пара с Qdrant на этом же сервере.

---

### Qwen2.5 (`qwen2.5:7b`) — подробно

**Qwen2.5** — линейка LLM от Alibaba; тег **`7b`** на этом хосте означает вариант ~7B параметров.

| Поле | Значение (с сервера) |
|------|----------------------|
| Полное имя | `qwen2.5:7b` |
| Семейство (Ollama `details.family`) | `qwen2` |
| Заявленный размер параметров | **7.6B** |
| Квантизация | **Q4_K_M** (компромисс качество/размер/скорость) |
| Размер файла модели | 4 683 087 332 байт (~4.36 GiB) |
| Digest | `845dbda0ea48ed749caafd9e6037047aa19acfcfd82e704d7ca97d631a0b697e` |

Использование через Ollama API: например `POST /api/generate` или OpenAI-совместимый `POST /v1/chat/completions` с полем `"model": "qwen2.5:7b"` (как принято в вашем клиенте).

---

### API (пример)

- `GET http://127.0.0.1:11434/api/tags` — полный JSON по всем моделям (включая `details.parameter_size`, `quantization_level`).
- `GET http://127.0.0.1:11434/api/version` — версия демона.
- В логах встречаются запросы к `/api/version`, `/v1/models` с внешних IP — сервис был доступен с сети при съёмке (порт открыт в UFW).

---

## 8. GPU

| Параметр | Значение |
|----------|----------|
| Устройство | **NVIDIA RTX A4000** |
| Драйвер | `535.288.01` (из `nvidia-smi`) |
| VRAM | **16376 MiB** всего |
| Занято VRAM (на съёмке) | **~11303 MiB** (активная нагрузка моделями/инференсом) |

Сервисы: **`nvidia-persistenced`** — active (типично для серверов с NVIDIA).

---

## 9. Прочие системные сервисы (не AI, но важны для эксплуатации)

На съёмке в состоянии **running** в том числе:

- `ssh` — удалённое администрирование  
- `docker`, `containerd` — контейнеры  
- `fail2ban` — защита от брутфорса SSH  
- `cron` — планировщик  
- `rsyslog` — логирование  
- `unattended-upgrades` — автообновления (контроль перезагрузок отдельно)  

---

## 10. Что на этом сервере **не** обнаружено (в снятом состоянии)

- **`pm2`** — не найден в PATH / не используется как менеджер процессов приложений.
- **`nginx`** — `systemctl is-active` → **inactive** (не в роли reverse-proxy на этом хосте).
- **`postgresql`** — **inactive** (нет локального Postgres в снятом списке).
- **`redis-cli`** — недоступен из окружения проверки (локального Redis не подтверждено).

При необходимости уточнить: `systemctl list-units --all | grep -Ei 'redis|postgres|mongo'`.

---

## 11. Типичные сценарии использования возможностей

1. **Генерация изображений / пайплайны ComfyUI** — UI и API на порту **8188** (снаружи по правилам UFW).
2. **RAG / поиск по знаниям** — Qdrant **6333/6334**, коллекции вида `asst_*`, `knowledge_chunks`.
3. **Локальные LLM и эмбеддинги** — Ollama **11434**, полный перечень и параметры — в **разделе 7** (всего **6** моделей, в т.ч. **Qwen2.5 7B**).

---

## 12. Как перепроверить документ на сервере

Выполнить на `188.124.55.89` (от root или с sudo):

```bash
hostname; uname -a; uptime; free -h; df -h /
ss -tlnp
docker ps -a
ufw status verbose
curl -s -o /dev/null -w "qdrant %{http_code}\n" http://127.0.0.1:6333/
curl -s http://127.0.0.1:6333/collections
ollama list
curl -s http://127.0.0.1:11434/api/tags | python3 -m json.tool
nvidia-smi
```

---

## История версий

| Дата | Изменение |
|------|-----------|
| 2026-04-04 | Первичная инвентаризация по SSH-съёмке |
| 2026-04-04 | Раздел Ollama: полная таблица всех 6 моделей, отдельный блок про **Qwen2.5 (`qwen2.5:7b`)**, уточнение по `api/tags` |
| 2026-04-05 | Убраны упоминания отдельного ComfyUI/порта под видео — в продукте пайплайн видео не используется |
